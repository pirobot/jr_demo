<?xml version="1.0"?>

<launch>

  <!-- We resume the logic in empty_world.launch, changing only the name of the world to be launched -->
  <include file="$(find jr_gazebo)/launch/empty_world.launch">
    <arg name="world_name" value="$(find jr_gazebo)/worlds/clearpath_playpen.world"/>
  </include>
  
  <include file="$(find jr_gazebo)/launch/spawn_jr.launch"/>

</launch>
